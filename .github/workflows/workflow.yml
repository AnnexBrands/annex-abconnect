name: Python Package CI/CD

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Upgrade Pip & Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # ruff pinned to match the `lint` dependency-group in pyproject.toml
          # so CI lint results are identical to local.
          python -m pip install build pytest ruff==0.15.5
          # Docs are built in CI with -W, so a broken toctree or reference is a
          # red build rather than something noticed months later on Read the Docs.
          python -m pip install sphinx sphinx-rtd-theme myst-parser
          # Editable install so `import ab` resolves to the checkout. The gate
          # evaluator and several tests locate tests/fixtures/ relative to
          # ab.__file__; a non-editable install would point them at
          # site-packages (where tests/ is not shipped) and fail spuriously.
          pip install -e .

      - name: Lint (ruff)
        run: ruff check .

      - name: Run tests (deterministic — excludes live API tests)
        run: pytest -m "not live"

      - name: Check progress report is current (no-drift gate)
        run: python scripts/generate_progress.py --check

      - name: Build docs (warning-as-error)
        run: sphinx-build -b html -W --keep-going docs docs/_build/html

  publish:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag'

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Verify tag matches package version
        run: |
          PKG_VERSION="$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")"
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          echo "tag=$GITHUB_REF_NAME (version $TAG_VERSION); pyproject=$PKG_VERSION"
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match pyproject.toml version $PKG_VERSION. Bump the version before tagging."
            exit 1
          fi

      - name: Remove Old Build Artifacts
        run: rm -rf dist build annex_abconnect.egg-info

      - name: Install Build Tools
        run: python -m pip install --upgrade build twine

      - name: Build Package
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
          skip-existing: true
