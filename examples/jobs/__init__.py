"""Job-scoped example scripts (one file per swagger subgroup).

Each module is a plain SDK script -- no runner indirection. Run with
``python -m examples.jobs.<subgroup>`` or ``ex jobs.<subgroup>`` from
the repo root.
"""
