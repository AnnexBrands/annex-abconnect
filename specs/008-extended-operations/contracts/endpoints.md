# SDK Interface Contracts: Extended Operations Endpoints

**Feature**: 008-extended-operations
**Date**: 2026-02-14

All methods follow the `**kwargs: Any` pattern from feature 007. Route objects are frozen dataclasses with `request_model`, `params_model`, and `response_model` strings.

## New Endpoint Classes

### RFQEndpoint (`api.rfq`) — 7 methods

```python
class RFQEndpoint(BaseEndpoint):
    def get(self, rfq_id: str) -> Any: ...
        # GET /rfq/{rfqId}
    def get_for_job(self, job_id: str) -> Any: ...
        # GET /rfq/forjob/{jobId}
    def accept(self, rfq_id: str, **kwargs: Any) -> Any: ...
        # POST /rfq/{rfqId}/accept
    def decline(self, rfq_id: str) -> Any: ...
        # POST /rfq/{rfqId}/decline
    def cancel(self, rfq_id: str) -> Any: ...
        # POST /rfq/{rfqId}/cancel
    def accept_winner(self, rfq_id: str) -> Any: ...
        # POST /rfq/{rfqId}/acceptwinner
    def add_comment(self, rfq_id: str, **kwargs: Any) -> Any: ...
        # POST /rfq/{rfqId}/comment
```

### ReportsEndpoint (`api.reports`) — 8 methods

```python
class ReportsEndpoint(BaseEndpoint):
    def insurance(self, **kwargs: Any) -> Any: ...
        # POST /reports/insurance
    def sales(self, **kwargs: Any) -> Any: ...
        # POST /reports/sales
    def sales_summary(self, **kwargs: Any) -> Any: ...
        # POST /reports/sales/summary
    def sales_drilldown(self, **kwargs: Any) -> Any: ...
        # POST /reports/salesDrilldown
    def top_revenue_customers(self, **kwargs: Any) -> Any: ...
        # POST /reports/topRevenueCustomers
    def top_revenue_sales_reps(self, **kwargs: Any) -> Any: ...
        # POST /reports/topRevenueSalesReps
    def referred_by(self, **kwargs: Any) -> Any: ...
        # POST /reports/referredBy
    def web2lead(self, **kwargs: Any) -> Any: ...
        # POST /reports/web2Lead
```

### DashboardEndpoint (`api.dashboard`) — 9 methods

```python
class DashboardEndpoint(BaseEndpoint):
    def get(self, **params: Any) -> Any: ...
        # GET /dashboard
    def get_grid_views(self) -> Any: ...
        # GET /dashboard/gridviews
    def get_grid_view_state(self, view_id: str) -> Any: ...
        # GET /dashboard/gridviewstate/{id}
    def save_grid_view_state(self, view_id: str, **kwargs: Any) -> Any: ...
        # POST /dashboard/gridviewstate/{id}
    def inbound(self, **kwargs: Any) -> Any: ...
        # POST /dashboard/inbound
    def in_house(self, **kwargs: Any) -> Any: ...
        # POST /dashboard/inhouse
    def outbound(self, **kwargs: Any) -> Any: ...
        # POST /dashboard/outbound
    def local_deliveries(self, **kwargs: Any) -> Any: ...
        # POST /dashboard/local-deliveries
    def recent_estimates(self, **kwargs: Any) -> Any: ...
        # POST /dashboard/recentestimates
```

### ViewsEndpoint (`api.views`) — 8 methods

```python
class ViewsEndpoint(BaseEndpoint):
    def list(self) -> Any: ...
        # GET /views/all
    def get(self, view_id: str) -> Any: ...
        # GET /views/{viewId}
    def create(self, **kwargs: Any) -> Any: ...
        # POST /views
    def delete(self, view_id: str) -> Any: ...
        # DELETE /views/{viewId}
    def get_access_info(self, view_id: str) -> Any: ...
        # GET /views/{viewId}/accessinfo
    def update_access(self, view_id: str, **kwargs: Any) -> Any: ...
        # PUT /views/{viewId}/access
    def get_dataset_sps(self) -> Any: ...
        # GET /views/datasetsps
    def get_dataset_sp(self, sp_name: str) -> Any: ...
        # GET /views/datasetsp/{spName}
```

### CommoditiesEndpoint (`api.commodities`) — 5 methods

```python
class CommoditiesEndpoint(BaseEndpoint):
    def get(self, commodity_id: str) -> Any: ...
        # GET /commodity/{id}
    def update(self, commodity_id: str, **kwargs: Any) -> Any: ...
        # PUT /commodity/{id}
    def create(self, **kwargs: Any) -> Any: ...
        # POST /commodity
    def search(self, **kwargs: Any) -> Any: ...
        # POST /commodity/search
    def suggestions(self, **kwargs: Any) -> Any: ...
        # POST /commodity/suggestions
```

### CommodityMapsEndpoint (`api.commodity_maps`) — 5 methods

```python
class CommodityMapsEndpoint(BaseEndpoint):
    def get(self, map_id: str) -> Any: ...
        # GET /commodity-map/{id}
    def update(self, map_id: str, **kwargs: Any) -> Any: ...
        # PUT /commodity-map/{id}
    def delete(self, map_id: str) -> Any: ...
        # DELETE /commodity-map/{id}
    def create(self, **kwargs: Any) -> Any: ...
        # POST /commodity-map
    def search(self, **kwargs: Any) -> Any: ...
        # POST /commodity-map/search
```

### NotesEndpoint (`api.notes`) — 4 methods

```python
class NotesEndpoint(BaseEndpoint):
    def list(self, **params: Any) -> Any: ...
        # GET /note  (params: category, jobId, contactId, companyId)
    def create(self, **kwargs: Any) -> Any: ...
        # POST /note
    def update(self, note_id: str, **kwargs: Any) -> Any: ...
        # PUT /note/{id}
    def suggest_users(self, **params: Any) -> Any: ...
        # GET /note/suggestUsers
```

### PartnersEndpoint (`api.partners`) — 3 methods

```python
class PartnersEndpoint(BaseEndpoint):
    def list(self) -> Any: ...
        # GET /partner
    def get(self, partner_id: str) -> Any: ...
        # GET /partner/{id}
    def search(self, **kwargs: Any) -> Any: ...
        # POST /partner/search
```

## Extended Endpoint Classes

### JobsEndpoint (`api.jobs`) — 24 new methods

```python
# ---- On-Hold (10 methods) ------------------------------------------------

def list_on_hold(self, job_display_id: int) -> Any: ...
    # GET /job/{jobDisplayId}/onhold
def create_on_hold(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/onhold
def delete_on_hold(self, job_display_id: int) -> Any: ...
    # DELETE /job/{jobDisplayId}/onhold
def get_on_hold(self, job_display_id: int, on_hold_id: str) -> Any: ...
    # GET /job/{jobDisplayId}/onhold/{id}
def update_on_hold(self, job_display_id: int, on_hold_id: str, **kwargs: Any) -> Any: ...
    # PUT /job/{jobDisplayId}/onhold/{onHoldId}
def get_on_hold_followup_user(self, job_display_id: int, contact_id: str) -> Any: ...
    # GET /job/{jobDisplayId}/onhold/followupuser/{contactId}
def list_on_hold_followup_users(self, job_display_id: int) -> Any: ...
    # GET /job/{jobDisplayId}/onhold/followupusers
def add_on_hold_comment(self, job_display_id: int, on_hold_id: str, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/onhold/{onHoldId}/comment
def update_on_hold_dates(self, job_display_id: int, on_hold_id: str, **kwargs: Any) -> Any: ...
    # PUT /job/{jobDisplayId}/onhold/{onHoldId}/dates
def resolve_on_hold(self, job_display_id: int, on_hold_id: str, **kwargs: Any) -> Any: ...
    # PUT /job/{jobDisplayId}/onhold/{onHoldId}/resolve

# ---- Email (4 methods) ---------------------------------------------------

def send_email(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/email
def send_document_email(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/email/senddocument
def create_transactional_email(self, job_display_id: int) -> Any: ...
    # POST /job/{jobDisplayId}/email/createtransactionalemail
def send_template_email(self, job_display_id: int, template_guid: str) -> Any: ...
    # POST /job/{jobDisplayId}/email/{emailTemplateGuid}/send

# ---- SMS (4 methods) -----------------------------------------------------

def list_sms(self, job_display_id: int) -> Any: ...
    # GET /job/{jobDisplayId}/sms
def send_sms(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/sms
def mark_sms_read(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/sms/read
def get_sms_template(self, job_display_id: int, template_id: str) -> Any: ...
    # GET /job/{jobDisplayId}/sms/templatebased/{templateId}

# ---- RFQ (2 job-scoped methods) ------------------------------------------

def list_rfqs(self, job_display_id: int) -> Any: ...
    # GET /job/{jobDisplayId}/rfq
def get_rfq_status(self, job_display_id: int, rfq_service_type: str, company_id: str) -> Any: ...
    # GET /job/{jobDisplayId}/rfq/statusof/{rfqServiceType}/forcompany/{companyId}

# ---- Freight (4 methods) -------------------------------------------------

def list_freight_providers(self, job_display_id: int, **params: Any) -> Any: ...
    # GET /job/{jobDisplayId}/freightproviders
def save_freight_providers(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/freightproviders
def get_freight_provider_rate_quote(self, job_display_id: int, option_index: int) -> Any: ...
    # POST /job/{jobDisplayId}/freightproviders/{optionIndex}/ratequote
def add_freight_items(self, job_display_id: int, **kwargs: Any) -> Any: ...
    # POST /job/{jobDisplayId}/freightitems
```

### LookupEndpoint (`api.lookup`) — 12 new methods

```python
# ---- Generic lookup (2 methods) ------------------------------------------

def get_by_key(self, key: str) -> Any: ...
    # GET /lookup/{masterConstantKey}
def get_by_key_and_id(self, key: str, value_id: str) -> Any: ...
    # GET /lookup/{masterConstantKey}/{valueId}

# ---- Named convenience methods (10 methods) ------------------------------

def get_access_keys(self) -> Any: ...
    # GET /lookup/accessKeys
def get_access_key(self, access_key: str) -> Any: ...
    # GET /lookup/accessKey/{accessKey}
def get_ppc_campaigns(self) -> Any: ...
    # GET /lookup/PPCCampaigns
def get_parcel_package_types(self) -> Any: ...
    # GET /lookup/parcelPackageTypes
def get_document_types(self) -> Any: ...
    # GET /lookup/documentTypes
def get_common_insurance(self) -> Any: ...
    # GET /lookup/comonInsurance
def get_density_class_map(self) -> Any: ...
    # GET /lookup/densityClassMap
def get_refer_categories(self) -> Any: ...
    # GET /lookup/referCategory
def get_refer_category_hierarchy(self) -> Any: ...
    # GET /lookup/referCategoryHeirachy
def reset_cache(self) -> Any: ...
    # GET /lookup/resetMasterConstantCache
```

### CompaniesEndpoint (`api.companies`) — 16 new methods

```python
# ---- Brands (2 methods) --------------------------------------------------

def get_brands(self) -> Any: ...
    # GET /companies/brands
def get_brands_tree(self) -> Any: ...
    # GET /companies/brandstree

# ---- Geo Settings (4 methods) --------------------------------------------

def get_geo_area_companies(self, **params: Any) -> Any: ...
    # GET /companies/geoAreaCompanies
def get_geo_settings(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/geosettings
def save_geo_settings(self, company_id: str, **kwargs: Any) -> Any: ...
    # POST /companies/{companyId}/geosettings
def get_global_geo_settings(self) -> Any: ...
    # GET /companies/geosettings

# ---- Carrier Accounts (4 methods) ----------------------------------------

def search_carrier_accounts(self, **params: Any) -> Any: ...
    # GET /companies/search/carrier-accounts
def suggest_carriers(self, **params: Any) -> Any: ...
    # GET /companies/suggest-carriers
def get_carrier_accounts(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/carrierAcounts
def save_carrier_accounts(self, company_id: str, **kwargs: Any) -> Any: ...
    # POST /companies/{companyId}/carrierAcounts

# ---- Packaging (6 methods) -----------------------------------------------

def get_packaging_settings(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/packagingsettings
def save_packaging_settings(self, company_id: str, **kwargs: Any) -> Any: ...
    # POST /companies/{companyId}/packagingsettings
def get_packaging_labor(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/packaginglabor
def save_packaging_labor(self, company_id: str, **kwargs: Any) -> Any: ...
    # POST /companies/{companyId}/packaginglabor
def get_inherited_packaging_tariffs(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/inheritedPackagingTariffs
def get_inherited_packaging_labor(self, company_id: str) -> Any: ...
    # GET /companies/{companyId}/inheritedpackaginglabor
```

### ContactsEndpoint (`api.contacts`) — 5 new methods

```python
def post_history(self, contact_id: str, **kwargs: Any) -> Any: ...
    # POST /contacts/{contactId}/history
def get_history_aggregated(self, contact_id: str) -> Any: ...
    # GET /contacts/{contactId}/history/aggregated
def get_history_graph_data(self, contact_id: str) -> Any: ...
    # GET /contacts/{contactId}/history/graphdata
def merge_preview(self, merge_to_id: str, **kwargs: Any) -> Any: ...
    # POST /contacts/{mergeToId}/merge/preview
def merge(self, merge_to_id: str, **kwargs: Any) -> Any: ...
    # PUT /contacts/{mergeToId}/merge
```

## Route Count Summary

| Class | Existing Routes | New Routes | Total |
|-------|----------------|------------|-------|
| RFQEndpoint | 0 (new) | 7 | 7 |
| ReportsEndpoint | 0 (new) | 8 | 8 |
| DashboardEndpoint | 0 (new) | 9 | 9 |
| ViewsEndpoint | 0 (new) | 8 | 8 |
| CommoditiesEndpoint | 0 (new) | 5 | 5 |
| CommodityMapsEndpoint | 0 (new) | 5 | 5 |
| NotesEndpoint | 0 (new) | 4 | 4 |
| PartnersEndpoint | 0 (new) | 3 | 3 |
| JobsEndpoint | 30 | 24 | 54 |
| LookupEndpoint | 4 | 12 | 16 |
| CompaniesEndpoint | 8 | 16 | 24 |
| ContactsEndpoint | 7 | 5 | 12 |
| **TOTAL** | **49** | **106** | **155** |
